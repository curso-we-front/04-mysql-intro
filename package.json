{
  "name": "04-mysql-intro",
  "version": "1.0.0",
  "description": "MySQL con Node.js — conexión, queries básicas y seeds",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/db/migrate.js",
    "seed": "node src/db/seed.js",
    "test": "jest --runInBand --forceExit"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql2": "^3.22.4"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
